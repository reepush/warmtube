{
  "name": "warmtube",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "server": "^1.0.15"
  },
  "scripts": {
    "deploy": "rsync -rvc --exclude node_modules --exclude .git . push.org.ru:www/files/warmtube"
  }
}
